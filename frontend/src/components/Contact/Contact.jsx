import React, { forwardRef } from 'react'
import './Contact.scss';

function Contact() {
    return (
        <div>
            <p>Contact  Section</p>
        </div>
    )
}

export default forwardRef(Contact);